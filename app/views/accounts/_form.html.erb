<div class="partial">
	<% @accounts.each do |a| %>
		<h1><%= link_to "#{a.name.capitalize}", "/accounts/#{a.id}", :class => 'navigateToContact' %></h1>
		<p><em>Name: </em><%= a.name %></p>
		<hr/>
	<% end %>
</div>