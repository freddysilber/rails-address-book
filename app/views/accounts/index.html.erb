<div class="indexContainer">
	<div class="contactsListContainer">
		<center><h1>My Accounts</h1></center>
		<hr/>
		<% @accounts.each do |a| %>
			<%= render partial: "accounts/account", locals: { a: a } %>
		<% end %>
	</div>

	<div class="indexNewForm">
		<center><h1>New Account</h1></center>
		<hr/>
		<%= render "form" %>
	</div>
</div>