<%= form_for @task do |t| %>
	<center>
		<%= label_tag(:task_name) %>
		<%=t.text_field :task_name, :placeholder => :task_name, :class => 'field' %>
		<br/>

		<%= t.submit 'Save', :class => 'button' %>
	</center>
<% end %>