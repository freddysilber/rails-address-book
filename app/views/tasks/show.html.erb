<center>
	<%= link_to "Back", tasks_path, :class => "button" %>
	<%= link_to "Delete", @task, method: :delete, data: { confirm: "Are you sure you want to delete task: #{@task.task_name}?"}, :class => "button", :style => "float: right" %>
	<%= link_to "Edit", "#{@task.id}/edit", :class => "button", :style => "float: right" %>
	<br/>
	<hr/>
	<br/>
	<%= render partial: "tasks/task", locals: { t: @task } %>
</center>