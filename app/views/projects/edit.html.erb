<center>
	<% if @project.errors.any? %>
		<%= render partial: "layouts/errors", locals: { r: @project } %>
	<% end %>

	<%= link_to "Back", project_path, :class => "button" %>
	<br/>
	<hr/>
	<br/>
	<div>
		<h1 class="title">Edit Project: <%= @project.project_name %></h1>
		<%= render "form" %>
	</div>
</center>