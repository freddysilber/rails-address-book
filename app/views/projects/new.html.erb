<%= link_to "Back", accounts_path, :class => "button" %>
<center>
	<% if @project.errors.any? %>
		<%= render partial: "layouts/errors", locals: { r: @project } %>
	<% end %>
	<hr/>
	
	<h1 class="title">New Project</h1>
	<hr/>
	<%= render "form" %>
</center>